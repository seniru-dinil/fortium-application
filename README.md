# Fortium Application Spring boot / MySQL / Docker

This project demonstrates a simple Spring Boot application with MySQL, containerized using Docker and managed with Docker Compose.

## Run Locally

```bash
mvn clean package
docker-compose up --build
